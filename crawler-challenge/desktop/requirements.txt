# Desktop Processing Requirements
# 데스크탑에서 Router/Processor/Storage 레이어 실행 시 필요한 의존성
aiokafka>=0.10.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
html2text>=2024.2.26
minio>=7.2.0
asyncpg>=0.29.0
msgpack>=1.0.7
zstandard>=0.22.0
python-dotenv>=1.0.0
prometheus-client>=0.20.0

# Embedding (RAG) 의존성
# 로컬 임베딩 모델 (EMBED_BACKEND=local, 기본값)
sentence-transformers>=3.0.0
# OpenAI 임베딩 API (EMBED_BACKEND=openai 시 필요)
openai>=1.0.0

# Search API 서버
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
