# Stream Pipeline Dependencies
# ============================
# pip install -r requirements-stream.txt

# HTTP Client (Layer 1)
httpx[http2]>=0.25.0

# Message Queue (All Layers)
aiokafka>=0.10.0
confluent-kafka>=2.3.0

# Compression (Layer 1)
zstandard>=0.22.0

# Object Storage (Layer 4)
minio>=7.2.0

# HTML Processing (Layer 2, 3)
beautifulsoup4>=4.12.0
lxml>=5.0.0
html2text>=2024.2.26

# Browser Automation - Rich Worker (Layer 3)
# Note: Run 'crawl4ai-setup' after installation
crawl4ai>=0.3.0

# Database (Layer 4)
asyncpg>=0.29.0
psycopg[binary,pool]>=3.1.0

# Serialization
msgpack>=1.0.7

# Monitoring
prometheus-client>=0.20.0
prometheus-async>=22.2.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
tenacity>=8.2.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Type checking
mypy>=1.7.0
types-beautifulsoup4>=4.12.0
